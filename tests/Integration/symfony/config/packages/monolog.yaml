monolog:
    handlers:
        main:
            type: stream
            path: "%kernel.logs_dir%/%kernel.environment%.log"
            level: debug
            channels: ["!event"]
        otel:
            type: service
            id: appsignal.monolog.otel_handler
            channels: ["app"]
